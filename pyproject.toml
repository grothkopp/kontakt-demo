[project]
name = "contact-demo"
version = "0.1.0"
description = "A small Django contact app for a quality and observability workshop"
requires-python = ">=3.13"
dependencies = ["django>=5.2,<5.3"]

[dependency-groups]
e2e = [
    "playwright>=1.63.0",
]
